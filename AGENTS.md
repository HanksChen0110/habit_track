# 项目协作约定

## 目标

这是一个个人自用的本地习惯记录与每周执行复盘工具。当前可实施规格以 `openspec/changes/build-habit-review-mvp/` 为准；不扩展账号、云同步、提醒、连续天数、长期热力图、社交或 AI 建议。

## 结构

- `docs/`：历史产品设计、实施计划和使用说明；其中标注为“已替代”的文档不得作为实现依据。
- `openspec/changes/build-habit-review-mvp/`：当前 MVP 的提案、设计、能力规格、任务与评审记录。
- 应用源码与测试在实施计划确认后再创建；遵循与其相邻的项目约定。

## 修改原则

- 只实现已确认范围；新增范围先更新设计文档并取得确认。
- 用户数据只保存在浏览器本地；不接入第三方服务。
- 修改后必须运行与改动相关的检查或测试，并报告结果。
- 不提交密钥、令牌或真实个人数据。

## 提交与高风险操作

- 目录初始化为 Git 仓库后，提交前先展示变更与验证结果。
- 删除文件、修改密钥或 CI/CD、数据库迁移、推送远端等操作必须先获得用户明确授权。
