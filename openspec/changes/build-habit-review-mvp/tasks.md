## 1. 前端基础与验证环境

- [ ] 1.1 选择并初始化依赖少、支持 TypeScript、静态部署和自动化测试的客户端 PWA 工具链，且不引入后端服务。
- [ ] 1.2 配置单元测试与浏览器交互测试入口，并验证测试命令可在干净环境运行。
- [ ] 1.3 配置 PWA 应用清单、应用壳缓存和更新策略，验证已加载应用在离线状态可打开。

## 2. 领域数据与统计计算

- [ ] 2.1 先为本地日期、周一至周日边界、当前周排除未来日期编写失败测试。
- [ ] 2.2 实现版本化 Store、习惯、完成记录和 `(habitId, date)` 复合唯一性领域模型。
- [ ] 2.3 先为 `activeOn(date)`、创建日/归档日和归档次日失效编写失败测试，再实现生命周期计算。
- [ ] 2.4 先为加减记录、归零删除、上限限制、`今天减 6 天` 至今天的编辑范围，以及创建当日外目标锁定编写失败测试，再实现记录命令。
- [ ] 2.5 先为部分完成量、历史周完整统计、当前周截至今天统计、无计划日期、可比较日期不足两天、并列最佳/最差日期和紧邻周同比编写失败测试，再实现周报纯函数。

## 3. 本地持久化与数据恢复

- [ ] 3.1 实现当前浏览器的单一版本化 Store 读取和写入，并测试刷新后恢复、同浏览器多标签页更新及读写失败不改变已持久化状态。
- [ ] 3.2 先为 JSON 导入校验失败案例编写测试，再实现版本、非空唯一 ID、名称、日期、引用关系、数值范围、归档区间和重复记录校验。
- [ ] 3.3 实现完整 JSON 导出、导入预览、明确确认后的原子替换和取消导入；测试任一失败都保留原 Store。
- [ ] 3.4 实现损坏本地数据和未知未来版本的恢复提示，验证应用不会静默初始化为空数据。

## 4. 今日记录与习惯管理界面

- [ ] 4.1 实现首次空状态和创建习惯流程，测试无效名称或目标不会改变数据。
- [ ] 4.2 实现今天页的“实际完成量 / 计划完成量”、习惯行和完成量加减控件，确保保存成功后才显示已保存反馈。
- [ ] 4.3 实现最近 7 天日期入口和“修正漏记”状态，测试不可编辑日期不会出现可操作控件。
- [ ] 4.4 实现名称编辑、归档确认和每日目标锁定提示，测试归档不删除历史记录。
- [ ] 4.5 实现导入导出管理入口及其成功、取消、校验失败和写入失败状态。

## 5. 周报与双端体验

- [ ] 5.1 实现本周和历史周浏览，展示整体执行率、每日执行率、无计划日期、最佳/最差日期、进行中状态与紧邻周缺失比较状态。
- [ ] 5.2 按 `design.md` 的低保真线框实现手机单列今天页、底部导航和不小于 44 CSS 像素的完成量控件。
- [ ] 5.3 实现桌面今天页与本周摘要侧栏，确保桌面端仍可完成每日记录和最近 7 天纠正。
- [ ] 5.4 为完成、部分完成、无数据、无计划、进行中和错误状态补充文字或数值表达，并进行键盘、焦点、辅助技术状态提示与不辨色可用性检查。

## 6. 端到端验证与试用准备

- [ ] 6.1 编写端到端场景：创建每日 1 次与多次习惯、记录当天、纠正历史、归档、刷新恢复、导出后导入恢复。
- [ ] 6.2 编写端到端错误场景：未来/过期日期、重复记录、损坏 JSON、未知版本、空/重复习惯 ID、孤儿记录和本地写入失败均不得破坏原数据。
- [ ] 6.3 在 320 CSS 像素和 1024 CSS 像素视口验证三项核心流程均可完成：记录、周报、管理。
- [ ] 6.4 完成静态部署前验证：无账号/网络请求依赖、PWA 安装与离线应用壳、本地数据不跨浏览器自动同步。
- [ ] 6.5 编写 21 天小范围试用说明：三位试用者各完成两次周复盘，在每次复盘后于试用记录中写下下一周的一项可观察执行调整，并在下一次复盘确认是否实际采用；该试用记录不进入产品数据模型。
