# review_test 约定

这是一个独立的静态视觉原型：只使用 `index.html`、`styles.css` 和 `app.js`，不依赖构建工具、网络字体或第三方资源。

- `index.html` 负责页面结构与可访问语义。
- `styles.css` 负责视觉令牌和响应式布局。
- `app.js` 只提供演示级交互，不存储真实用户数据。
- 修改时以 `../openspec/changes/build-habit-review-mvp/UI-spec.md` 的视觉边界为准。
